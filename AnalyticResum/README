The analytic resummed calculations are generated in Mathematica.  First Mathematica outputs a bunch of txt files, and then the txt2yoda.py script (which is easiest to call from alltxt2yoda.sh) makes the yoda files.  Only the yoda files need to be stored.